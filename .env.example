# SQLite (Default for zero-config local run)
DATABASE_URL="file:./dev.db"

# PostgreSQL (For Neon / Supabase / Railway production deployment)
# DATABASE_URL="postgresql://username:password@ep-host.region.pooler.neon.tech/swiftmile?sslmode=require"

JWT_SECRET="your-256-bit-secret-key-here"
NEXT_PUBLIC_APP_NAME="SwiftMile Logistics"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional Email / SMS Notification Provider settings (e.g. Resend, Twilio, SendGrid)
# EMAIL_PROVIDER_API_KEY=""
# SMS_PROVIDER_API_KEY=""
